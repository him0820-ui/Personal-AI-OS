<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalai.os.mapper.MemoryConflictMapper">

    <select id="selectPendingByUserId" resultType="com.personalai.os.entity.MemoryConflict">
        SELECT * FROM memory_conflict WHERE user_id = #{userId} AND review_status = 'PENDING' ORDER BY created_at DESC
    </select>

    <select id="selectByUserId" resultType="com.personalai.os.entity.MemoryConflict">
        SELECT * FROM memory_conflict WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="countPendingByUserId" resultType="int">
        SELECT COUNT(*) FROM memory_conflict WHERE user_id = #{userId} AND review_status = 'PENDING'
    </select>

</mapper>
